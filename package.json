{
  "name": "ar-daily-visits",
  "version": "1.0.0",
  "private": true,
  "description": "Auto Republic — Reporte diario de visitas",
  "scripts": {
    "start": "serve -s . -l tcp://0.0.0.0:${PORT:-3000} --no-clipboard"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
